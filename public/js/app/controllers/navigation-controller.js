 define(['controllers/base/controller', 'models/navigation', 'views/navigation-view'], function(Controller, UserModel, MessageModel, UserView, MessageForm, MessageView) {

    'use strict';
var NavigationController = Controller.extend({
	
});
	return NavigationController;
});